.ui.two.column.middle.aligned.relaxed.fitted.stackable.grid
  .right.aligned.column
    %h1
      %i.sign.in.icon
      Already have an account? Check-in to your everytrack.
    .ui.right.aligned.segment
      -#
        %a.ui.google.plus.primary.button{href: "/"}
          %i.google.plus.icon
          Login with G+
      %a.ui.green.button{href: "/users/sign_in"}
        Check-in
  .column
    .ui.card
      .content
        = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
          = devise_error_messages!
          .two.fields
            .required.field
              = f.label :email
              .ui.icon.input
                = f.email_field :email, autofocus: true, placeholder: "Email"
                %i.user.icon
            %br/
            .required.field
              %label Password
              .ui.icon.input
                = f.password_field :password, autocomplete: "off", placeholder: "Password"
                %i.lock.icon
              %label Confirm Password (also, memory verification)
              .ui.icon.input
                = f.password_field :password, autocomplete: "off", placeholder: "Password again?"
                %i.lock.icon
            %br/
            = f.submit "Sign up", {class: "ui green basic button"}
      .extra.content
        %a.right.floated.created
        %a{:href => "http://twitter.com/SarupBanskota"} Tweet to @SarupBanskota
